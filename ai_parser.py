def ai_parse_reminder(command):
    """
    AI fallback for reminder commands.

    This function will eventually send difficult
    commands to a small AI model and return a
    structured reminder.

    For now, it returns None so that the normal
    parser remains in control.
    """

    return None